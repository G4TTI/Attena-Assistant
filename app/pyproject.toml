[project]
name = "attena-assistant"
version = "1.1.0-alpha"
description = "Attena Assistant — agendador de mensagens WhatsApp sobre o WAHA, com calendário e automações"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
filterwarnings = ["ignore::DeprecationWarning"]
