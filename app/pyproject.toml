[project]
name = "whatsapp-scheduler"
version = "0.1.0"
description = "Agendador de mensagens WhatsApp sobre o WAHA"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
filterwarnings = ["ignore::DeprecationWarning"]
